[project]
name = "rwv-test-types"
version = "1.0.0"
description = "Test fixture for repoweave e2e registry tests"
license = "MIT"
requires-python = ">=3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
